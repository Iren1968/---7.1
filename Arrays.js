let balance = 500;
let codeFromSms = 'A001DFX0';
const postamat = [null, null, null, 'A001DFX0', null];

if ((indexCell = codeFromSms)) {
  balance = balance - 100;
  let indexCell = postamat.indexOf('A001DFX0') + 1;
  console.log(`Parcel received from ${indexCell}, balance ${balance}`);
}
